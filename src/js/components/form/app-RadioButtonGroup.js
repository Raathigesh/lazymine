/** @jsx React.DOM */
var React = require('react');

var RadioButtonGroup = React.createClass({
    render : function(){
        return (
            <div>

            </div>
        );
    }
});

module.exports = RadioButtonGroup;